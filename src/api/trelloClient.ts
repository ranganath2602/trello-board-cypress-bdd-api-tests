// THIS FILE IS DEPRECATED
// The Trello helper functions were moved to `src/archived/trelloClient.ts` after refactoring
// step definitions to use inline `cy.request(...)` with aliases + assertions.
// Keep a minimal module here to avoid breaking imports; prefer inline requests in steps.

export {};